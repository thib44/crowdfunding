<div class="container">
  <%# tag(:div, class: "fb-share-button", data: { href: request.original_url, layout: "button_count" } ) %>
   <div class="fb-share-button" data-href="<%= request.original_url %>" data-layout="button_count"></div>

  <% @projets.in_groups_of(3,false) do |records| %>
    <div class="row">
      <div>

        <%= render records %>
      </div>
    </div>
  <% end %>
  <div class="text-center">
    <%= paginate @projets %>
  </div>

</div>

