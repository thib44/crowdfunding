<nav class="navbar-wagon">
  <div class="container navbar-wagon-container">

   <%= link_to root_path do %>
      <i class="fa fa-home fa-4x"></i><strong>Home</strong>
    <% end %>
    <div class="navbar-wagon-pusher"></div>

      <!-- Choix de la langue -->
      <div class="dropdown">
        </i><strong data-toggle= "dropdown">Language</strong>

        <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
          <li>
            <%= link_to change_language_path('en') do %>
              <%= t ".profile", default: "English" %>
            <% end %>
          </li>
          <li>
            <%= link_to change_language_path('fr') do %>
              <%= t ".profile", default: "French" %>
            <% end %>
          </li>
        </ul>
      </div>
      <hr>

    <% if user_signed_in? %>
      <div class="navbar-wagon-item">
        <div class="dropdown">
          <i class="fa fa-user fa-2x" data-toggle= "dropdown"></i><strong data-toggle= "dropdown">Menu</strong>

          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li>
              <%= link_to root_path do %>
                <i class="fa fa-home"></i>  <%= t ".profile", default: "Home" %>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Log out" %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    <hr>
    <%= link_to 'Create a project', new_projet_path, class: "navbar-wagon-item navbar-wagon-button btn" %>
    <% end %>
  </div>
</nav>
